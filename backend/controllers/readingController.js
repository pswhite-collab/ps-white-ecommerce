export const getReadingProgress = async (_req, res) => {
  return res.status(501).json({ message: 'Reading progress endpoint placeholder for Day 2' });
};

export const updateReadingProgress = async (_req, res) => {
  return res.status(501).json({ message: 'Update reading progress endpoint placeholder for Day 2' });
};

export const addBookmark = async (_req, res) => {
  return res.status(501).json({ message: 'Add bookmark endpoint placeholder for Day 2' });
};
